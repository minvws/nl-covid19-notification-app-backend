jobs:  
  - job: Build_App_IksUploader
    displayName: Publish Build Artifact Iks.Uploader
    steps:
      - task: UseDotNet@2
        displayName: 'Install .NET Core SDK'
        inputs:
          version: 3.1.x        
      - task: DotNetCoreCLI@2
        displayName: 'dotnet publish'
        inputs:
          command: 'publish'
          publishWebProjects: false
          projects: 'src/Iks.Uploader/Iks.Uploader.csproj'
          # Version suffix starts with build number and Append the current git hash to the version suffix
          arguments: '-o $(Build.ArtifactStagingDirectory)/$(Apps.IksUploader.ArtifactSubDirectory) --no-self-contained --runtime $(runtime) --configuration $(buildConfiguration) --version-suffix $(Build.BuildNumber)-$(Build.SourceVersion)'
          # CIBG don't use ZIP files for artifacts
          zipAfterPublish: false            
      - task: PublishBuildArtifacts@1
        inputs:
          PathtoPublish: '$(Build.ArtifactStagingDirectory)/$(Apps.IksUploader.ArtifactSubDirectory)'
          ArtifactName: $(Publish.ArtifactName)
          publishLocation: 'Container'